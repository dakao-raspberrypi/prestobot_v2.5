<!-- <launch>
    <!-- File Paths -->
    <arg name="urdf_path" default="$(find-pkg-share prestobot_description)/urdf/prestobot.urdf.xacro" />

    <!-- Simulation arguments -->
    <arg name="is_sim" default="false" />

    <!-- Let variables for cleaner paths and commands -->
    <let name="robot_description" value="$(command 'xacro $(var urdf_path) is_sim:=$(var is_sim)')" />
    <let name="rviz_config_path" value="$(find-pkg-share prestobot_description)/rviz/urdf_config.rviz" />

    <!-- Robot State Publisher -->
    <node pkg="robot_state_publisher" exec="robot_state_publisher" >
        <param name="robot_description" value="$(var robot_description)" />
    </node>

    <node pkg="controller_manager" exec="ros2_control_node">
        <param from="$(find-pkg-share prestobot_controller)/config/prestobot_controller.yaml" />
    </node>

    <node pkg="controller_manager" exec="spawner" args="joint_state_broadcaster" />
    <node pkg="controller_manager" exec="spawner" args="diff_drive_controller" />

    <node pkg="rviz2" exec="rviz2" output="screen" args="-d $(var rviz_config_path)" />

</launch> -->